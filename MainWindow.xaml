<Window x:Class="SteamVault.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:SteamVault.Views"
        Title="SteamVault" Height="720" Width="1080"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBackground}"
        Foreground="{StaticResource Text}">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" />
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" /> <!-- Custom Title Bar -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0" Background="{StaticResource SidebarBackground}">
            <Grid>
                <TextBlock Text="STEAMVAULT" FontWeight="Bold" FontSize="12" Foreground="{StaticResource MutedText}" 
                           VerticalAlignment="Center" Margin="16,0" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Content="—" Width="46" Height="32" Click="Minimize_Click" Background="Transparent" Foreground="{StaticResource MutedText}" BorderThickness="0" FontSize="12" />
                    <Button Content="✕" Width="46" Height="32" Click="Close_Click" Background="Transparent" Foreground="{StaticResource MutedText}" BorderThickness="0" FontSize="12" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" /> <!-- Sidebar -->
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="{StaticResource SidebarBackground}">
                <StackPanel Margin="0,16,0,0">
                    <Button Content="Dashboard" Command="{Binding NavigateDashboardCommand}" Style="{StaticResource NavButton}" />
                    <Button Content="Settings" Command="{Binding NavigateSettingsCommand}" Style="{StaticResource NavButton}" />
                </StackPanel>
            </Border>

            <!-- Main Content Area -->
            <ContentControl Grid.Column="1" Content="{Binding CurrentView}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type viewmodels:DashboardViewModel}" xmlns:viewmodels="clr-namespace:SteamVault.ViewModels">
                        <views:DashboardView />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}" xmlns:viewmodels="clr-namespace:SteamVault.ViewModels">
                        <views:SettingsView />
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>
    </Grid>
</Window>
