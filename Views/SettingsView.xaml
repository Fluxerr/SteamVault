<UserControl x:Class="SteamVault.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Settings" Style="{StaticResource TitleText}" Grid.Row="0"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,16,0">

                <!-- Database Sync -->
                <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="24" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Key &amp; Token Database" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding DatabaseStats}" Foreground="{StaticResource MutedText}" FontSize="13" Margin="0,0,0,16"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="{Binding SyncStatusMessage}" TextWrapping="Wrap" Foreground="{StaticResource MutedText}" VerticalAlignment="Center" FontSize="13" Margin="0,0,16,0"/>
                            <Button Grid.Column="1" Content="🔄  Update Database" Command="{Binding SyncDatabaseCommand}" Width="160" Height="36"/>
                        </Grid>
                        
                        <TextBlock Text="Syncs depotkeys.json and appaccesstokens.json from GitHub to support the latest 2026 releases. No API key needed." Foreground="{StaticResource DimText}" FontSize="12" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Local Directories -->
                <Border Background="{StaticResource CardBackground}" CornerRadius="12" Padding="24">
                    <StackPanel>
                        <TextBlock Text="Local Directories" FontWeight="Bold" FontSize="16" Margin="0,0,0,16"/>
                        
                        <TextBlock Text="Steam Main Directory" Style="{StaticResource SubtitleText}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Settings.SteamDirectory}" Height="36" VerticalContentAlignment="Center" Margin="0,0,0,16" Tag="e.g. C:\Program Files (x86)\Steam"/>
                        
                        <TextBlock Text="Lua Files Target (.lua)" Style="{StaticResource SubtitleText}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Settings.LuaOutputPath}" Height="36" VerticalContentAlignment="Center" Margin="0,0,0,16" Tag="e.g. ...\Steam\config\lua"/>
                        
                        <TextBlock Text="Manifest Cache (.manifest)" Style="{StaticResource SubtitleText}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Settings.ManifestCachePath}" Height="36" VerticalContentAlignment="Center" Margin="0,0,0,0" Tag="e.g. ...\Steam\config\depotcache"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" Foreground="#4CAF50" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="Save Settings" Command="{Binding SaveCommand}" Width="150"/>
        </Grid>
    </Grid>
</UserControl>
